name: SAX-043 Final Diagnostics

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  diagnose:
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.13"
          cache: pip
      - run: sudo apt-get update && sudo apt-get install --yes ffmpeg
      - run: python -m pip install --upgrade pip && python -m pip install -e ".[dev]"
      - name: Capture quality output
        run: |
          set +e
          python scripts/check_quality.py > sax043-final-quality.log 2>&1
          echo "exit_code=$?" >> sax043-final-quality.log
          exit 0
      - uses: actions/upload-artifact@v6
        with:
          name: sax043-final-quality-diagnostics
          path: sax043-final-quality.log
          retention-days: 1
